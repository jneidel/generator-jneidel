const expect = require( "expect" );
const <%= moduleName %> = require( "./index" );

/* globals describe it */

describe( "<%= moduleName %>", () => {
  it( "", () => {
    expect( )
      .toBe( );
  } );
} );
